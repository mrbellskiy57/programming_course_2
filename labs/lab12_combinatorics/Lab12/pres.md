


### Код алгоритма 

```cpp
№1  #include <iostream>
№2  using namespace std;

№3  int *P, *R, n;

№4  void per(int k) {
№5      for (int i = 1; i <= n; i++) {
№6          if (R[i] == 0) { 
№7              P[k] = i; R[i] = 1;
№8              
№9              if (k == n) { 
№10                 for (int j = 1; j <= n; j++) cout << P[j] << " ";
№11                 cout << endl;
№12             } else per(k + 1);          
№13             R[i] = 0;          
№14         }
№15     }
№16 }

№17 int main() {
№18     n = 3;
№19     P = new int[n + 1]; 
№20     R = new int[n + 1]{0};
№21     per(1);
№22     return 0;
№23 }
```

---

### Шаг 0: Объявление глобальных переменных
В статической памяти резервируется место под указатели и переменную n.

```cpp
№3  int *P, *R, n;
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 0 | | |
| **R** = nullptr | | |
| **P** = nullptr | | |

---

### Шаг 1: Инициализация
Задаем `n = 3`. Выделяем массивы в куче. В стек добавляется новый вызов функции (main).

```cpp
№18     n = 3;
№19     P = new int[n + 1]; 
№20     R = new int[n + 1]{0};
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **main()** |
| **R** = (адрес) | **R** = [0, 0, 0, 0] | |
| **P** = (адрес) | **P** = [?, ?, ?, ?] | |

---

### Шаг 2: Выбор первого числа
Вызываем `per(1)`. В стек попадает вызов функции с параметром `k = 1`. Запускается цикл, находим первое свободное число `i = 1`.

```cpp
№21     per(1);
// --- Внутри per(1) ---
№5      for (int i = 1; i <= n; i++) { 
№6          if (R[i] == 0) {           // R[1] == 0 (свободно)
№7              P[k] = i; R[i] = 1;    // P[1] = 1, R[1] = 1
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **k = 1, i = 1** |
| **R** = (адрес) | **R** = [0, 1, 0, 0] | main() |
| **P** = (адрес) | **P** =[?, 1, ?, ?] | |

---

### Шаг 3: Выбор второго числа (Рекурсия)
Вызываем `per(2)`. Для второй позиции число 1 уже занято (`R[1] == 1`), поэтому берем следующее свободное число `i = 2`.

```cpp
№12             } else per(k + 1);     // Вызов per(2)
// Внутри per(2)
№5      for (int i = 1; i <= n; i++) { // -> i = 2
№7              P[k] = i; R[i] = 1;    // P[2] = 2, R[2] = 1
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **k = 2, i = 2** |
| **R** = (адрес) | **R** =[0, 1, 1, 0] | k = 1, i = 1 |
| **P** = (адрес) | **P** = [?, 1, 2, ?] | main() |

---

### Шаг 4: Выбор третьего числа и вывод первой перестановки
Вызываем `per(3)`. Свободна только тройка (`i = 3`). Так как мы заполнили массив (`k == n`), выводим первую комбинацию на экран.

```cpp
№12             } else per(k + 1);     // вызов per(3)
// Внутри per(3)
№5      for (int i = 1; i <= n; i++) { // i = 3
№7              P[k] = i; R[i] = 1;    // P[3] = 3, R[3] = 1
№9              if (k == n) {          // k=3 
№10                 ... cout << P[j];  // вывод: 1 2 3
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **k = 3, i = 3** |
| **R** = (адрес) | **R** = [0, 1, 1, 1] | k = 2, i = 2 |
| **P** = (адрес) | **P** = [?, 1, 2, 3] | k = 1, i = 1 |

---
### Шаг 5: Откат
`R[3] = 0`. Цикл в `per(3)` заканчивается(i=3). Функция завершается и удаляется из стека.

```cpp
№13             R[i] = 0;              // R[3] = 0
№14         }
№15     }                              // Вышли из per(3)
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | |  |
| **R** = (адрес) | **R** =[0, 1, 1, 0] | **k = 2, i = 2** |
| **P** = (адрес) | **P** =[?, 1, 2, 3] | k = 1, i = 1 |

---

### Шаг 6: Продолжение перебора
Вернулись в `per(2)`, далее `R[2] = 0`. Цикл делает шаг `i = 3`. Ставим тройку на второе место.

```cpp
// в per(2)
№13             R[i] = 0;              // R[2] = 0 
№5      for (int i = 1; i <= n; i++) { // i = 3
№7              P[k] = i; R[i] = 1;    // P[2] = 3, R[3] = 1
№12             ... else per(k + 1);   // вызов per(3)
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **k = 2, i = 3** |
| **R** = (адрес) | **R** =[0, 1, 0, 1] | k = 1, i = 1 |
| **P** = (адрес) | **P** = [?, 1, 3, 3] | main() |

---

### Шаг 7: Снова per(3) и вывод второй перестановки
Единица и тройка заняты. берем `i = 2`. Далее k=n, выводим результат.

```cpp
// внутри per(3)
№5      for (int i = 1; i <= n; i++) { // i = 2
№7              P[k] = i; R[i] = 1;    // P[3] = 2, R[2] = 1
№9              if (k == n) {          // k=3
№10                 ... cout << P[j];  // вывод: 1 3 2
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **k = 3, i = 2** |
| **R** = (адрес) | **R** = [0, 1, 1, 1] | k = 2, i = 3 |
| **P** = (адрес) | **P** =[?, 1, 3, 2] | k = 1, i = 1 |

---

### Шаг 8: Завершение первой ветви
После вывода 1 3 2: `R[2] = 0`, выходим из `per(3)`. Возвращаемся в `per(2)`: `R[3] = 0`. Цикл в `per(2)` завершен, выходим.

```cpp
// в per(3) 
№13             R[i] = 0;               
// в per(2)
№13             R[i] = 0;              
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | |  |
| **R** = (адрес) | **R** =[0, 1, 0, 0] |  |
| **P** = (адрес) | **P** = [?, 1, 3, 2] | **k = 1, i = 1** |

---

### Шаг 9: Смена первого элемента в per(1)
Мы оказались в самом начале (`per(1)`). Освобождаем `R[1]`. `i` становится 2.

```cpp
//в per(1)
№13             R[i] = 0;              // R[1] = 0.
№5      for (int i = 1; i <= n; i++) { // i = 2
№7              P[k] = i; R[i] = 1;    // P[1] = 2, R[2] = 1
№12             ... else per(k + 1);   // Вызов per(2)
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **k = 1, i = 2** |
| **R** = (адрес) | **R** =[0, 0, 1, 0] | main() |
| **P** = (адрес) | **P** = [?, 2, 3, 2] | |

---

### Шаг 10: Формирование новой комбинации в per(2)
`i = 1` свободно, берем на вторую позицию.

```cpp
// в per(2)
№5      for (int i = 1; i <= n; i++) { // i = 1
№6          if (R[i] == 0) {           // R[1] == 0 
№7              P[k] = i; R[i] = 1;    // P[2] = 1, R[1] = 1
№12             ... else per(k + 1);   //  per(3)
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **k = 2, i = 1** |
| **R** = (адрес) | **R** =[0, 1, 1, 0] | k = 1, i = 2 |
| **P** = (адрес) | **P** = [?, 2, 1, 2] | main() |

---

### Шаг 11: Вывод третьей перестановки
Единица и двойка уже находятся в массиве флагов (`R[1] = 1`, `R[2] = 1`). Свободна только тройка, берем ее.

```cpp
// в per(3)
№5      for (int i = 1; i <= n; i++) { //  i = 3
№7              P[k] = i; R[i] = 1;    // P[3] = 3, R[3] = 1
№9              if (k == n) {          // 3 
№10                 ... cout << P[j];  // вывод: 2 1 3
```

| Статическая память | Динамическая память | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | **k = 3, i = 3** |
| **R** = (адрес) | **R** =[0, 1, 1, 1] | k = 2, i = 1 |
| **P** = (адрес) | **P** = [?, 2, 1, 3] | k = 1, i = 2 |



